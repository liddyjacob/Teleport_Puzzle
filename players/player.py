#player .py
import pygame

from drawutils import *

class Player:
	def __init__(self):
		self.test = "haha"

	def update(self, inputs, gamemap, objects):
		return
	
	def draw(self, drawutils, screen)
		return
